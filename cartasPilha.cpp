#include <iostream>
#include "pilhaDinamica.h"
#include "filaDin.h"

using namespace std;
int main(){
   int n;
   //TStack pilha;
   //TSqueue fila;
   cin >> n;
   //pilha.empilhar(0);
   /*for(int i = n; i > 0; i--){
      pilha.empilhar(i);
   }*/
   /*while(pilha.profundidade() >= 2){
      pilha.desempilhar();
      while(pilha.topo != 0){
         fila.emfilheirar(pilha.topo());
         pilha.desempilhar();
      }
      while(!fila.vazia()){
         pilha.empilhar(fila.cabeca());
         fila.desemfilheirar();
      }
   }*/
   return 0;
}
